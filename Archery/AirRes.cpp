//#include "AirRes.h"
//
//float AirRes::DragForce(vector<float> u, vector<float> v, float c1, float c2)
//{
//	float first = c1 * (DotProduct(u, v) / Length(v));  // c1 * ||u x v|| / ||v||
//	float second = c2 * (DotProduct(u, v) / Length(v));
//	
//	return sqrt(first * first + second * second);
//}
//
